version: "3.8"

services:
  crypto-radar:
    build: .
    container_name: crypto-radar
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
    environment:
      - TZ=Europe/Moscow
      - PYTHONUNBUFFERED=1
